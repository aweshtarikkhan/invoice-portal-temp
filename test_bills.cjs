const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:8081/bills', { waitUntil: 'networkidle2' }); await page.type('input[type=email]', 'awesh.etpl@gmail.com'); await page.type('input[type=password]', '123456@Ak'); await page.click('button[type=submit]'); await page.waitForNavigation({ waitUntil: 'networkidle2' }); await page.goto('http://localhost:8081/bills', { waitUntil: 'networkidle2' }); await page.waitForSelector('tbody tr', { timeout: 5000 }); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', err => console.log('PAGE ERROR:', err.toString())); await page.click('tbody tr:first-child'); await new Promise(r => setTimeout(r, 2000)); console.log('Current URL:', page.url()); const content = await page.content(); if (content.includes('Loading...')) console.log('Stuck on Loading...'); else console.log('Content length:', content.length); await browser.close(); })();
